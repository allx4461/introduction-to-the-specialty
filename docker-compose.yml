services:
  aquarium:
    build: ./bulbul        
    ports:
      - "8080:8080"        
    restart: always
  informer:
    build: ./informer      
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build: ./web
    ports:
      - "80:80"
    restart: always